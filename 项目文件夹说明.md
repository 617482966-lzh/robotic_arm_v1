# 项目文件夹说明

本文档说明项目根目录中每个文件夹的作用、是否参与当前 V3 打包，
以及能否清理。

## 目录总览

| 文件夹 | 主要用途 | V3 是否使用 | 清理建议 |
|---|---|---:|---|
| `apps/` | 可独立启动的图形界面程序 | 是 | 必须保留 |
| `data/` | 标定数据、CSV 和运行日志 | 否 | 确认备份后再清理 |
| `demo/` | 厂家 JSON/Modbus 通信示例及命令行示例 | 否 | 建议保留作协议参考 |
| `pdf/` | 机械臂与传感器说明书 | 否 | 建议保留 |
| `picture/` | 项目公共图标源文件 | 间接使用 | 建议保留 |
| `release/` | PyInstaller 生成的可发布程序 | 成品目录 | 可重新生成 |
| `robot_client/` | 机械臂 JSON 通信与运动控制 | 是 | 必须保留 |
| `sensor_2/` | 二通道传感器驱动 | 否 | V2 使用，必须归档保留 |
| `sensor_6/` | 六维力/力矩传感器驱动 | 是 | 必须保留 |
| `tmp/` | PDF 提取或调试产生的临时文件 | 否 | 确认无用后可清理 |
| `tools/` | 独立标定和维护工具 | 否 | 建议保留 |
| `__pycache__/` | Python 字节码缓存 | 否 | 可删除，会自动生成 |
| `.git/` | Git 提交历史、分支和版本控制数据 | 否 | 不要删除 |
| `.idea/` | PyCharm 项目设置 | 否 | 不影响 EXE，可按需保留 |
| `.agents/` | Codex/代理相关项目元数据 | 否 | 建议保留 |

## `apps/`

每套可以独立启动的桌面程序放在单独子目录中。

### `apps/sensor_2_control_v2/`

归档的二通道传感器控制界面 V2，包含自己的：

- `main.py`
- `main_window*.py`
- `main_window*.ui`
- `picture/`
- `README.md`
- `sensor_2_control_v2.ini`：V2 运行后生成的位置记忆，不纳入 Git；

该目录调用根目录的 `robot_client/` 和 `sensor_2/`。当前 V3 打包
不会调用它，但不能删除，否则无法重新运行二通道版本。

### `apps/sensor_6_control_v3/`

当前主要程序，使用六维力传感器。

关键文件：

- `main.py`：程序入口、线程、试验流程、数据采集和 XLSX 保存；
- `main_window_3.py`：V3 完整窗口、图表、信号和界面逻辑，不依赖
  `main_window.py`；
- `main_window_3.ui`：V3 Qt Designer 界面；
- `main_window.py`、`main_window.ui`：历史基础界面文件，仅保留参考；
- `sensor_6_control_v3.spec`：Windows EXE 打包配置；
- `picture/`：该程序发布时使用的 JLU 图标；
- `README.md`：V3 功能说明。
- `sensor_6_control_v3.ini`：V3 运行后生成的位置和试验参数记忆，
  不纳入 Git。

当前 EXE 正是由这个目录的 `main.py` 启动。

## `data/`

保存速度标定、角度标定和在线测试产生的数据，例如：

- `penetration_angle_speed_*.csv`
- `angle_run_*.log`
- `z_angle_run_*.log`

该目录不是程序默认试验数据保存位置，也不会进入 V3 EXE。
带有 `INVALID` 的数据表示已判定无效，不应作为最终标定结果。

## `demo/`

厂家协议和早期调试示例，包括：

- `json_read.py`
- `json_write.py`
- `robot_cli.py`
- `modbus_read.py`

`demo` 只作为通信格式与调用方法参考。正式图形界面不把它当作
函数库，机械臂正式功能应放在 `robot_client/`。

## `pdf/`

保存项目硬件说明书，包括：

- 伯朗特 HC1 远程通信协议；
- 伯朗特远程指令培训教材；
- 六维力传感器说明书；
- 二通道传感器说明书。

这些文件不进入 EXE，但修改机械臂 action 或传感器寄存器前应优先
查阅，不能用经验猜测协议。

## `picture/`

保存项目公共的：

- `jlu.png`
- `jlu.ico`

当前 V3 程序目录中也保存了一份发布资源。修改软件图标后，应同步
检查 `apps/sensor_6_control_v3/picture/`，然后重新打包。

## `release/`

保存最终可复制到其他电脑的发布程序。

当前结构：

```text
release/
└─ sensor_6_control_v3/
   ├─ sensor_6_control_v3.exe
   ├─ sensor_6_control_v3.ini  （首次运行后生成）
   ├─ 使用说明.txt
   └─ _internal/
```

`release/` 是构建产物，不是源代码。执行带 `--noconfirm` 的
PyInstaller 命令时，同名发布目录会被重新生成。

复制程序到其他电脑时，必须复制整个 `sensor_6_control_v3/`，
不能只复制 EXE。

需要迁移记忆值时，应同时复制 `sensor_6_control_v3.ini`。重新打包会
覆盖同名发布目录，打包前应先备份已经使用过的 INI。

## `robot_client/`

机械臂正式控制模块。

- `hc1_json.py`：TCP 连接、JSON 收发、状态读取和底层 action；
- `robot_control.py`：界面调用的高层运动控制、速度换算、掩码计算；
- `calibrate_penetration_angles.py`：独立标定脚本，不由 V3 自动运行；
- `mock_robot_server.py`：离线模拟服务，不由 V3 自动运行。

V3 只通过 `robot_control.py` 和它依赖的 `hc1_json.py` 控制机械臂。
修改这里的代码后必须重新打包。

## `sensor_2/`

二通道传感器通信模块：

- `communication.py`
- `controller.py`

通信参数由该目录维护，不应写入 V2 主界面。当前 V3 不调用此目录。

## `sensor_6/`

六维力传感器正式通信模块：

- `communication.py`：串口、115200、8N1、Modbus-RTU 等通信管理；
- `controller.py`：寄存器读取、Fx/Fy/Fz/Tx/Ty/Tz 解析和通道清零；
- `README.md`：独立调用方式和协议说明；
- `__init__.py`：Python 包标识。

V3 直接调用这个目录。串口号由界面提交，波特率等协议参数由
`sensor_6` 内部统一维护。修改后必须重新打包。

## `tmp/`

保存说明书解析、PDF 页面提取等临时文件。该目录不进入 EXE。
清理前确认其中没有尚未整理的协议截图或解析结果。

## `tools/`

保存不属于主界面的独立维护脚本。

当前 `calibrate_penetration_speed.py` 用于贯入速度标定，不会在
打开 V3 软件时自动运行，也不会进入 V3 的正常操作流程。

## `__pycache__/`

Python 自动生成的 `.pyc` 缓存。它不是源码，可以删除。打包配置
以 `.py` 文件为准，不应依赖这里的缓存文件。

## `.git/`

Git 数据库，保存所有提交、分支和恢复历史。删除后项目将失去版本
记录和回溯能力。不要手工修改其中内容。

## `.idea/`

PyCharm 的解释器、运行配置和窗口设置。删除不会影响源代码和 EXE，
但会丢失本机 IDE 配置。不同电脑的绝对路径可能不同。

## `.agents/`

用于 Codex 或自动化代理的项目级元数据。它不参与程序运行和打包，
但建议随项目保留，便于后续继续自动化开发。
